{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Compose: Up (dev)",
      "type": "node-terminal",
      "request": "launch",
      "command": "./scripts/docker-compose.sh --profile dev up --build"
    },
    {
      "name": "Compose: Up (prod)",
      "type": "node-terminal",
      "request": "launch",
      "command": "./scripts/docker-compose.sh --profile prod up --build"
    }
  ]
}